# D-027 フェーズ5: AI が MCP 経由で動作確認シナリオを完遂(初回実証)

- **決定/結果**: AI Agent(Claude)が MCP ツール(statee_cli)だけでスイカゲームを操作し、
  以下のシナリオを完遂した。フェーズ5の完了条件を最初のシナリオについて満たした。
  1. seed=12345 で起動 → 初期 State 確認(Score 0 / FruitCount 0 / NextKind Cherry)
  2. pause → drop → step 60×2 で着地を決定論的に観測(Y 586.7 → 744.3)
  3. 同種2個目を drop → step 120 → **合体を検証**: Score +1(Strawberry の三角数)、
     Id 0,1 消滅・Id 2 Strawberry 生成、logs に drop/スコアの証跡
  4. resume → 同一 x へ連投 → 6投目で **IsGameOver=true**(溢れ猶予 1.0s 経過)
  5. ゲームオーバー後の drop が「ゲームオーバー中は投下できない」エラー(**盤面凍結**)
  6. quit で正常終了(exit 0)
- **フレームワークへの気づき**:
  - 「着地(安定)を待つ」を step 固定フレーム + state ポーリングで代用した。
    **条件待機コマンド(GUIDELINE §7-1)の必要性を実地で確認**。
    「State のフィールドが条件を満たすまで step する」形が有効そう
  - 盤面の速度情報(静止判定)が State にないため「安定した」を Y の不変で推測した。
    Fruits に速度 or IsSleeping を足すと待機条件が書きやすくなる
- **状態**: 初回シナリオ ✅。次はシナリオの拡充(狙い撃ち合体の連鎖、UI/幾何検証)と
  条件待機の実装

