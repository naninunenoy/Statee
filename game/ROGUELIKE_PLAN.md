# RogueGame 計画(仮称。形になったらこのファイルは削除する)

第2サンプルゲーム: emoji タイルのターン制ミニローグライク(→ docs/adr/D-044.md)。
スイカゲームと検証特性が対極(ターン制・論理・決定論)のターゲットとして、
Statee / ゲーム開発ヘルパーの共通要素を別方向から蓄積する。

## ゲーム仕様

- **構成**: 5 フロアを降りて最下層の 💎 を取り、階段を戻って地上へ帰還したら勝ち
  (5 分程度のワンセッション)
- **帰路の演出**: 💎 取得で各フロアに敵が増援され、全敵が追跡モードに覚醒する。
  往路で温存したアイテムを帰路で使う駆け引きを作る
- **戦闘**: bump-to-attack。HP + 攻撃力のみ、**乱数なしの完全決定論**
  (乱数はダンジョン生成のシードにのみ使う)
- **アイテム**: 3〜4 種(🧪 回復 / 🗡️ 武器強化 / 🗝️ 鍵と施錠扉 等)+ インベントリ
  (拾う / 使う)
- **敵 AI**: 視界内なら 1 歩近づく直進追跡、視界外は静止。決定論的
- **視界**: 探索済み地形は記憶表示、Entity(敵・アイテム)は視界内のみ表示。
  視界判定は敵 AI の「見えたら追跡」と共用する
- **生成**: 部屋+通路の古典的生成。シードから完全再現可能
- **描画**: emoji タイル。Noto Color Emoji(OFL)をフォントとして同梱し、
  グリッドに文字を置くだけで描画する。画像・音・3D アセットは使わない

## アーキテクチャ

- スイカゲームと同じ境界(D-023, D-024, D-031, D-043): 純C#層に
  生成・ターン処理・戦闘・インベントリ・視界のすべてを置き、
  Godot 層は emoji 描画とキー入力 → アクション変換のみ
- ゲーム全体が「アクション列 → 状態遷移」の決定論的な関数になるよう設計する
- 帰路の増援・覚醒のため、離脱したフロアの状態を保持する(フロアは使い捨てない)

## Statee での検証の柱

1. アクション(移動・攻撃・拾う・使う)ごとの State 逐次照合(D-043 の入力 API の主用途)
2. シード固定でのダンジョン生成再現性(headless で同一シード → 同一 State)
3. プレイログ(アクション列)のリプレイ検証 — 記録したアクション列の再生が
   同一結果になることの確認。決定論設計の強みを検証機能として昇華する

## 進め方

GUIDELINE.md の 4 段階(スケルトン → 失敗するテスト → 実装 → リファクタ)で進める。

## 進捗

純C#層(`game/RogueGame.Logic` + `tests/RogueGame.Logic.Tests`、81テスト)は完了:

- [x] シード付きダンジョン生成(部屋+通路。同一シード→同一マップ、階段間の到達可能性)
- [x] プレイヤー移動・階段によるフロア遷移・フロア状態の保持
- [x] 敵の配置生成・視界判定(Bresenham)・直進追跡の敵ターン
- [x] bump-to-attack 戦闘・敵の攻撃・ゲームオーバー
- [x] アイテム(🧪 回復 / 🗡️ 攻撃強化)・インベントリ・使用ターン
- [x] 💎 取得 → 訪問済み全フロアに増援+全敵覚醒 → 持ち帰りでクリア

Godot 層(`game/RogueGame.Godot`)も完了。headless + Statee での疎通・move/use・
実入力経路(key)・シード再現性を E2E 確認済み:

- [x] Godot 層: emoji タイル描画(DrawString + FontVariation フォールバック)・
      Noto Color Emoji サブセット同梱(約20KB。assets/fonts/README.md に再生成手順)・
      キー入力 → アクション変換(バインド表を game/input に公開)
- [x] Fog of War 描画(探索済み記憶=減光表示+視界内のみ Entity。State は全公開)
- [x] Statee 組み込み: game/rogue State(Map/Player/Enemies/Items)、
      ping / move / use / key / screenshot / quit コマンド、--seed 起動

- [x] E2E: エージェントによるフルクリア(seed=12345、画面あり、HP 7/10 残しで
      IsCleared=true。State だけを頼りに経路計画 → move 送信 → 照合のループで達成)
- [x] リプレイ検証(検証の柱3): RogueLogic が全入力を ActionLog に記録、
      Apply / Replay で再生(全87テスト緑)。Godot 層は game/rogue/actions として公開し、
      E2E で「プレイ → 記録取得 → 同一シード再起動 → 再生 → State 完全一致」を確認済み

残り:

- [ ] 人間がプレイして5分ゲームとして成立するかの調整(敵数・HP・フロア構成)。
      知見: 敵は等速の直進追跡なので逃げ続ける限り被弾しない。帰路の緊張感が課題
- [ ] 鍵と施錠扉(🗝️)は任意。コアループ成立後に検討
